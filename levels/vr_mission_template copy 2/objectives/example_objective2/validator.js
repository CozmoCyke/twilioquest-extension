module.exports = async helper => {
  if(helper.validationFields == 'print(model.predict([10.0]))')
  helper.success("Believe it or not, you covered most of the concepts in ML that you'll use in far more complex scenarios. CONGRATULATIONS!!")
};